# v1.3.2

`v1.3.2`는 `v1.3.1` 이후 실플레이와 문맥 검수에서 확인된 대사 행 경계/구조 문제와 Stage 4 번역 품질을 집중적으로 다듬은 안정화 릴리스입니다. 전역 대사를 기계적으로 다시 번역하거나 존대/반말을 일괄 치환하지 않고, 원문 행 경계와 실제 화자·청자 관계가 확인된 구간만 좁게 수정했습니다. SaveRAM 형식은 변경하지 않았으며 모든 승격 단계에서 live SaveRAM을 byte-exact로 보존했습니다.

## 주요 변경 사항

### 전역 대사 행 경계 / 20셀 의미 복구

- v1.3.1 이후 원문 물리 행과 현재 한글 runtime 행을 다시 대조해, 이전 reflow 과정에서 의미가 다음 행으로 섞이거나 일부가 잘린 구간을 재검수했습니다.
- `global_dialogue_boundary_retranslation` 패스에서 실제 변경된 행은 **115개**입니다.
  - runtime contract 기반 시나리오 행: **88개**
  - raw 경로 행: **27개**
  - 이 중 bank59 현재 runtime 행: **8개**
  - 기존 contract에서 빠졌던 시나리오 행: **19개**
- 구조용 leading `0x18`은 문자처럼 일괄 제거하지 않고, 주소별로 입증된 control-prefix 근거가 있는 경우에만 구조로 처리하도록 감사 로직을 보강했습니다.
- 최종 전역 20셀 감사에서 **16,629개 표시 행 모두 20셀 이하**, unreadable 0, semantic guard failure 0을 확인했습니다.

### Stage 4 조니 라이덴 / 가토 문맥 교정

- Stage 4의 조니 라이덴·샤아·가토 관련 대사를 원문과 주변 화자 문맥으로 다시 검토했습니다.
- **14개 문구**를 수정하고, 기존 extractor가 놓쳤던 1개 레코드도 현재 runtime 경로에서 복구했습니다.
- `붉은 혜성`, `진홍의 번개`, 가토의 계급 및 화자별 말투가 문맥상 어색하지 않도록 정리했습니다.

### Stage 4 후반부 문맥 전면 재번역

Stage 4 후반부는 단문 교정이 아니라 장면 단위로 다시 번역했습니다.

- 미안의 사이드 3 귀환 장면과 샤아·브라드 대화
  - 초기 후반부 문맥 패스에서 **57개 표시 행**을 수정했습니다.
  - 미안의 `……아빠。 / 사이드3에서 기다릴게。 / 꼭…… / 꼭 돌아와 줘。` 흐름을 원문에 맞게 정리했습니다.
  - 샤아가 브라드에게 말하는 상관 어조, 브라드가 샤아에게 답하는 존대 관계를 장면 안에서 일관되게 맞췄습니다.
  - 가토의 계급 표기를 문맥에 맞게 `가토 대위`로 교정했습니다.
- 미안 장면 후속 재검수
  - 기계번역 느낌이 남아 있던 샤아·브라드 대화를 추가로 **11개 표시 행** 수정했습니다.
  - `아이 취급하지 말고 이치를 설명하면 / 납득하는 법일세。`, `……나도 어릴 적엔 그랬지。` 등으로 자연스럽게 다듬었습니다.
  - 특수 runtime 레코드 `60B400`은 과거 실측으로 안전성이 확인된 `E51D5101` 본체를 그대로 보존하고 helper 문구만 `……예？`로 교체했습니다.
- 기렌–데긴 솔라 레이 대화
  - 솔로몬 함락 보고부터 히틀러 비유가 끝나는 장면까지 **23개 표시 행**을 전면 재번역했습니다.
  - 데긴→기렌은 공왕·부친의 권위 있는 평어체, 기렌→데긴은 격식을 갖춘 존대체로 통일했습니다.
  - 대표적으로 `……그렇소。`를 **`……그렇습니다。`**로 수정하고, 솔라 레이 계획·전후 인구 정책·히틀러 비유 후반의 직역체도 문맥에 맞게 정리했습니다.

### 기체명 `파라스 아테네` 통일

- MS 도감에서는 이미 `파라스 아테네`였지만 name75 표시 경로에 `팔라스・아테네`가 남아 있던 불일치를 수정했습니다.
- 현재 표시:
  - MS 도감: `파라스 아테네`
  - name75 일반명: `파라스・아테네`
  - name75 랭크 마커형: `파라스・아테네█`
- 도감/게임 내 명칭 교차 감사에서 해당 충돌이 제거되었고, 자동으로 판단하기 어려운 나머지 항목은 임의 통일하지 않았습니다.

### runtime 감사 / 검증 로직 보강

- `dialogue_runtime_contracts.py`와 전역 20셀·미번역·bank59 감사기를 현재 runtime 구조에 맞게 갱신했습니다.
- 구조적 일본어처럼 보이는 바이트와 실제 사용자 가시 미번역을 분리해, 확인되지 않은 control byte를 번역 대상으로 오판하지 않도록 했습니다.
- 현재 canonical runtime contract는 **24,954건**입니다.
- 전역 존대/반말을 자동 정규화하는 방식은 오탐 가능성이 높아 v1.3.2 정본에는 사용하지 않았습니다. Stage 4처럼 화자·청자 관계가 확인된 장면만 문맥 기준으로 수정했습니다.

## 최종 검증

v1.3.2 메인TIP에서 다음을 확인했습니다.

- 메인TIP 크기: `16,777,216` bytes
- 메인TIP SHA-256: `E91CDE50CBE15386561495FB53FD51C26A279AD0614AAD57811D0169EFBC0BDB`
- WonderSwan checksum: `2B76` · 유효
- dialogue runtime contracts: `24,954`건
- active checked: `7,395`건
- quarantine checked: `17,559`건
- hard failure: `0`
- review item: `0`
- 전역 20셀 감사: `16,629`행 / over 20 `0` / unreadable `0` / semantic guard failure `0`
- 확정 사용자 가시 미번역 residual: `0`
- terminology audit: clean
- encyclopedia name cross-reference audit: 302 candidates / auto-unify 0 / review-required 11
- `tools/test_dialogue_runtime_safety_gate.py`: **5 / 5 PASS**
- `tools/test_dialogue_runtime_contracts.py`: **6 / 6 PASS**
- xdelta 원본 → 메인TIP round-trip: byte-exact PASS
- VCDIFF header indicator: `0x00`
- VCDIFF secondary compression/application header: 배포 헤더에서 비활성

## 배포 파일

- `monoeye_ko_expanded_v1.3.2.xdelta`
- xdelta SHA-256: `F7651DFB452CC17F49F454A7E1601A43BAFD33302451009295CD986BFE3E3CDB`
- 크기: `1,619,507` bytes
- xdelta round-trip: PASS

패치는 **합법적으로 소유한 일본판 원본 ROM**에 적용해야 합니다.

지원 원본 SHA-256:

`376E4C6B4B81CC3A7DCEB15DC4B7D0AF04D3E6C8B81E8572569C39D3394870A0`

정상 적용 후 ROM SHA-256:

`E91CDE50CBE15386561495FB53FD51C26A279AD0614AAD57811D0169EFBC0BDB`

## 세이브 호환성

v1.3.2는 ROM 데이터 수정이며 SaveRAM 형식 변경은 없습니다. v1.3.1에서 사용하던 세이브를 그대로 사용할 수 있습니다. 이번 릴리스에 포함된 모든 메인 승격 단계에서 live SaveRAM이 byte-exact로 보존되었음을 확인했습니다. 업데이트 전에는 원본 ROM과 세이브 파일을 별도로 백업하는 것을 권장합니다.

## 참고

- 이번 릴리스의 도감/인명 감사에서는 명확한 정본이 확인된 `파라스 아테네`만 통일했습니다. 다른 표기 후보는 임의 변경하지 않았습니다.
- 전역 존대/반말 혼용 자동 감사 결과는 오탐이 많아 릴리스 번역 기준으로 사용하지 않았습니다.
- v1.3에서 기록한 ID 커맨드 `선제` 우측 하단 잔여 타일 관련 알려진 이슈는 별도 실측 이슈로 유지합니다.
