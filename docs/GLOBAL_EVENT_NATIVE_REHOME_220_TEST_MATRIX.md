# 게임 전체 exact4 Event Error 위험 220건 — 대표 실측 매트릭스

작성일: 2026-08-17  
후보 ROM: `out/patch/global_event_native_rehome_220_candidate.wsc`  
SHA-256: `714200FFDCAD34D01C12C8F560B8CA71163C165803E5E9894FEB30F523E166C6`  
paired SaveRAM: `sram/global_event_native_rehome_220_candidate.sav`

> 2026-08-17 런타임 결과: 사용자 실측에서 **#2 Gato/콜로니 `가토오오오！！` 구간 PASS**, **#6 STAGE22t 웃소/카테지나 구간 PASS**를 확인했다. 전수 정적 게이트와 이 두 핵심 대표 경로를 승격 근거로 충분하다고 사용자 승인하여 12:10 KST 메인 TIP 승격 완료. 나머지 구간은 필수 승격 게이트가 아니라 후속 회귀 확인용으로 남긴다.

## 목적

220건을 하나씩 실측하지 않는다.

정적 감사에서 220건 전체에 대해 다음이 이미 증명됐다.

- record body extent 4바이트 유지
- terminator 원본 위치 유지
- double-NUL separator 유지
- 직후 `08/17` 이벤트 제어 바이트 유지
- 현재 한글 렌더 결과 유지
- top-level direct `E5 18` 위험 패턴 `220 -> 0`
- 전체 runtime contract 24,925건 hard failure `0`
- battle audit failure `0`
- bank64–69 unknown event/data diff `0`

따라서 실측은 **저장 경로와 이벤트 제어 패턴을 대표하는 7구간**만 확인한다.

## 공통 PASS 조건

각 구간에서 다음만 확인한다.

- [ ] Event Error가 발생하지 않는다.
- [ ] 대사 뒤에 `がけはう`, `こ`, 한자/히라가나 제어행 같은 글리프가 노출되지 않는다.
- [ ] 다음 대사가 건너뛰거나 반복 재생되지 않는다.
- [ ] 화자명/초상/캐릭터 표시가 깨지지 않는다.
- [ ] 아래 기준 대사가 정상 한글로 표시된다.

---

## 1. Bank60 혼합 경로 — `6094D0–6098B0`

**커버:** 5 targets / native 3 + parameterized E51D 2 / P1·P2·P3 모두 포함

주요 기준 대사:

- `6094D0` — `세레인……`
- `60973A` — `세라……`
- `609753` — `아까　그걸……` — **parameterized / continuation(P3)**
- `609769` — `아아……` — **parameterized / 17 28(P1)**
- `6098B0` — `세라……`

확인:

- [ ] 위 대사들이 정상 출력된다.
- [ ] `아까　그걸……` 및 `아아……` 뒤 이벤트가 자연스럽게 이어진다.
- [ ] Event Error / 제어문 노출 없음.

## 2. Gato/콜로니 구간 — `61035E–610444`

**커버:** 3 targets / **parameterized E51D 3건 전부** / P1 2 + P2 1

이 구간은 새 65건 wrapper 경로의 가장 중요한 스트레스 테스트다.

주요 기준 대사:

- `61035E` — `가토오오오！！`
- `6103C8` — `가토오오오！！`
- `610444` — `으윽……`

확인:

- [ ] `가토오오오！！` 두 번 모두 정상.
- [ ] `으윽……` 정상.
- [ ] 각 대사 직후 다음 이벤트/전투 흐름 정상.
- [ ] 화자/초상 깨짐 없음.

## 3. 카미유/포우 계열 밀집 구간 — `61B641–61C72F`

**커버:** 12 targets / native 8 + parameterized 4 / P1 11 + P2 1

주요 기준 대사:

- `61B641` — `으으……`
- `61B7D8` — `카미유……`
- `61B829` — `그래……` — **parameterized**
- `61BA93` — `이게……` — **parameterized**
- `61BDC0` — `시그……`
- `61C416` — `그런가……` — **parameterized**
- `61C72F` — `그러고　보니……` — **parameterized / P2**

확인:

- [ ] 위 짧은 대사 뒤 `17 28` 제어행이 화면에 새지 않는다.
- [ ] `그러고　보니……` 뒤 `08` 계열 전환 정상.
- [ ] 대화 반복/스킵/초상 오류 없음.

## 4. 데빌 건담/하만 이후 혼합 구간 — `624CD3–6260C6`

**커버:** 13 targets / native 9 + parameterized 4 / P1·P2·P3 모두 포함

이 구간에는 직전 batch01에서 따로 보려던 `도몬……` 2건도 포함된다. 별도 batch01 실측은 필요 없다.

주요 기준 대사:

- `624CD3` — `뭐라고！？`
- `624DFB` — `후후후후……`
- `6252E1` — `레인……`
- `6256CC` — `도몬……`
- `625730` — `도몬……`
- `6257ED` — `으윽……！` — **continuation(P3)**
- `625B7D` — `어째서　여기　있어！？` — **parameterized**
- `625BE5` — `알테이시아……` — **parameterized / P2**
- `625DF3` — `그게……` — **parameterized / P2**
- `6260C6` — `누오오옷！！` — **parameterized / P1**

확인:

- [ ] `도몬……` 두 장면 모두 정상.
- [ ] parameterized 4개 짧은 대사 정상.
- [ ] 데빌 건담 관련 이벤트 전환이 끊기지 않는다.

## 5. Bank63 기존 민감 구간 — `63411E–6355B9`

**커버:** 14 targets / native 10 + parameterized 4 / P1·P2·P3 포함

과거 STAGE21 계열의 `がけはう`/후속 제어행 문제가 있었던 bank63 인근이므로 우선도가 높다.

주요 기준 대사:

- `63411E` — `이런！？`
- `634328` — `하지만……`
- `63459D` — `제로……`
- `634910` — `컥！！` — **parameterized / P2**
- `634C59` — `아하하하하핫！！` — **parameterized**
- `634E61` — `대체　이런　일이！！` — **parameterized / continuation(P3)**
- `6351BD` — `샤아……`
- `6355B9` — `아하하하하핫！！` — **parameterized**

확인:

- [ ] 히라가나/제어행 노출이 없다.
- [ ] 초상/화자 상태가 다음 대사까지 정상이다.
- [ ] 이벤트가 반복되거나 중단되지 않는다.

## 6. STAGE22t 웃소/카테지나 회귀 확인 — `638CB1–638D79`

**커버:** 기존 runtime-proven **2-byte E51D index 0** 경로

이번 220건용 generalized dispatcher가 기존 STAGE22 수정도 공유하므로 반드시 한 번 확인한다.

기준:

- 카테지나의 웃소에 대한 대사
- `……어？`
- 직후 웃소 표시/대사
- 다음 카테지나 이벤트 진행

확인:

- [ ] Event Error `12288 / 36067` 재발 없음.
- [ ] `……어？` 정상.
- [ ] 직후 웃소 이름/대사 깨짐 없음.
- [ ] 이후 카테지나 이벤트 정상 진행.

## 7. 최종전/최종 출격 밀집 구간 — `63E5B7–63FDBF`

**커버:** 24 targets / native 23 + parameterized 1 / P1 11 + P2 12 + P3 1

가장 많은 target을 한 번에 확인할 수 있는 구간이며, 다양한 `08xx` 전환을 포함한다.

주요 기준 대사:

- `63E5B7` — `부탁한다！！`
- 다수의 연속 말줄임표 대사 (`63E5CE` 이후)
- `63F000` — `그건……`
- `63F5E9` — `레이라……`
- `63FD3A` — `응……` — **parameterized / P2**
- `63FDBF` — `미안……`

확인:

- [ ] 최종 출격/최종전 이벤트 흐름이 끝까지 정상이다.
- [ ] 연속 말줄임표 대사 뒤 제어행 노출 없음.
- [ ] `응……` 뒤 `08` 전환 정상.
- [ ] Event Error / 초상 깨짐 / 반복 진행 없음.

---

## 실측 보고 방식

모두 정상이면 다음처럼 짧게 보고하면 충분하다.

`1~7 전부 PASS`

문제가 있으면 번호와 증상만 적는다.

예: `4번에서 알테이시아…… 뒤 Event Error 발생`

초기 계획은 7구간 전체 PASS를 승인 기준으로 삼는 것이었으나, 실제로는 신규 parameterized 경로를 대표하는 #2와 기존 E51D index0 경로를 대표하는 #6이 모두 PASS했고 전수 정적 감사가 green이어서 사용자가 이를 충분한 런타임 승인 근거로 판단했다. 후보는 2026-08-17 12:10 KST 메인 TIP으로 승격되었으며, 나머지 구간은 선택적 후속 회귀 테스트다.
