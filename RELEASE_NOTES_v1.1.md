# v1.1

`v1.1`은 `v1.0.1` 이후 실플레이에서 확인된 시나리오/전투 구조 오류, 20셀 초과 잘림, 용어 불일치와 확장 사전 alias 충돌 위험을 누적 정리한 안정화 릴리스입니다. 새 번역 범위를 넓히기보다 이미 번역된 화면이 원문 구조와 게임 런타임 규칙에 맞게 끝까지 안정적으로 표시되도록 후속 검수와 감사 도구를 강화하는 데 초점을 맞췄습니다.

## 주요 변경 사항

### 시나리오 이벤트/제어 상태 안정화

- STAGE16t에서 확인된 대사 중복, 제어문 노출, 기계번역 잔재와 다음 화자 초상 오류를 수정했습니다.
- 짧은 시나리오 first-record를 일반 ext3 문구로 치환했을 때 페이지/초상 상태가 달라질 수 있는 사례를 native 문법으로 복구했습니다.
- `62:BF78`처럼 정적 구조상 위험해 보여도 실측에서 정상으로 확인된 사례는 runtime-confirmed 예외로 기록하여 이후 자동 수정 대상에서 제외했습니다.

### `최후의 승리자` / STAGE17t 후속 검수

- `최후의 승리자` 구간의 bank59 대사와 스프라이트 대사를 전수 재검수하여 잘림과 문맥 오역을 교정했습니다.
- 리리나의 호칭을 앞 문맥과 맞춰 `오라버니……？`로 통일했습니다.
- 젝스/리리나 장면에서 ID 커맨드용 문구가 잘못 출력되던 테스트 후보 문제를 분석해, five-page ext3 alias 영역을 일반 빈 슬롯으로 재사용하지 못하도록 빌더에 fail-closed 가드를 추가했습니다.
- STAGE17t에서 다음 문장 의미가 앞 레코드에 섞여 들어간 semantic spill을 원래 2레코드 구조로 복구했습니다.

### 전역 20셀 hard gate

- 기존 20셀 검사가 일부 review subset에만 적용되어 bank59 이벤트/스프라이트 대사가 빠지던 문제를 수정했습니다.
- 현재 구조가 확정된 사용자 가시 텍스트를 대상으로 다음 범위를 통합 검사합니다.
  - bank59 이벤트/스프라이트: 1,718건
  - banks 60–63 시나리오/작전·전황: 14,378건
  - 총 16,096건
- 실제 런타임 기준 20셀 초과 60건을 원문 의미를 유지하도록 재번역해 모두 20셀 이하로 정리했습니다.
- 최종 후보/메인TIP 기준 감사 결과는 `over_20 = 0`, `unreadable = 0`, semantic guard failure `0`입니다.
- 오래된 0-page alias 해석으로 14건이 false positive로 잡히던 감사 오류도 수정해 현재 five-page runtime 규칙을 반영했습니다.

### 용어/명칭 표준화

- `상크 킹덤` 계열을 `생크 킹덤`으로 통일했습니다.
- `파프테마스 시로코` 계열을 `팝티머스 시로코`로 통일했습니다.
- `도렌`/`드레인` 계열을 `드렌`으로 통일했습니다.
- `海ヘビ` 계열 무장명을 `바다뱀`으로 정리했습니다.
- 함선 `ラーディッシュ`의 프로젝트 표준을 **`라디쉬`**로 재지정하고 `래디시`, `라디시`, `라디슈`, `래디슈` 등의 변형을 활성 번역 소스, 일반 사전, five-page alias 사전과 실제 표시 레코드에서 제거했습니다.

### 2026-08-16 후속 실측 반영

- 필/디아나 교신 구간에서 `디아나 님!`이 다음 문장과 합쳐지거나 `こ` 같은 제어문이 노출되는 문제를 원본 native page-boundary 구조와 대조해 수정했습니다.
- 같은 이벤트의 문맥을 `저희들은 지구만을 생각하고, 달을`로 교정했습니다.
- `디아나 님……!!` 직후 다른 영역 데이터가 대사창에 출력되던 문제는 해당 대사 자체가 아니라 정상 분기 목적지인 `60:3F33~3F91`의 8개 제어 레코드가 과거 번역 과정에서 대사로 오분류되어 덮인 것이 원인이었습니다. 8개 레코드를 일본판 원본과 byte-exact로 복구하고, `603F45`를 포함한 해당 특수 영역이 다시 일반 대사 배치에 들어가지 않도록 감사/소스 경로를 정리했습니다.
- `하리` 계열은 `해리`, `항게르그/한겔구/한게르그` 계열은 `한겔그`, `기임/기암/김・깅가남` 계열은 `김 깅가남`으로 통일했습니다.
- `V2 어설트 버스터`와 `햄머 햄머` 뒤 MA 아이콘이 반으로 깨지는 회귀를 과거 갸프랑 사례와 같은 중복 TBL raw-code 평탄화 문제로 확인하고 `E736` 타일 정체성을 복구했습니다.
- 고정폭 UI의 `射全`은 기존 `근전`과 같은 compact-glyph 경로를 사용해 `사전`으로 한글화했습니다.
- 위 수정까지 누적한 최종 v1.1 메인TIP에서 terminology/name audit clean, runtime contract 24,925건 hard failure/review 0, 전역 20셀 16,096건 초과 0, 구조 감사 PASS를 다시 확인했습니다.

### ROM 자원/그래픽 안정성

- bank69에 남아 있던 원본과 다른 고립 1바이트 변경 7곳을 일본판 원본과 byte-exact하게 복구했습니다.
- 복구는 번역/대사 데이터가 아닌 후반 그래픽/자원 계열에 대한 원본 회복 성격이며, 후보 실측과 checksum 검증 후 누적했습니다.

### 구조 감사 강화

- runtime contract 24,925건을 다시 생성/감사하여 hard failure `0`, review item `0`을 확인했습니다.
- 전역 시나리오 구조 감사에서 runtime-proven native-only 주소와 과거 초상/metadata 수정 패밀리의 회귀가 없는지 확인했습니다.
- 전투대사 0F/5D metadata의 실측 증명 패밀리와 가토 초상 수정 anchor를 재검증했습니다.
- 미확정 battle quarantine 레코드는 정적 모양만으로 자동 수정하지 않는 정책을 유지합니다.

## 실측 확인

v1.1 후보에서 다음 항목을 실제 플레이로 확인했습니다.

- STAGE16t 이벤트 대사, 초상, 제어문 및 이벤트 진행 정상
- `최후의 승리자`의 잘림 교정, 젝스/리리나 대화, 리리나 `오라버니……？` 정상
- STAGE17t의 `제단의 문` 작전/전황 문장과 후속 대사 정상
- `여기는 헨켄！ / 라디쉬도 문제없다！！` 표기 정상
- five-page alias 충돌로 ID 커맨드 문구가 시나리오에 잘못 출력되던 문제 재발 없음
- 전역 20셀 교정 후 해당 검수 구간 진행/초상/스프라이트 이상 없음
- 필/디아나 교신 이벤트의 페이지 분리 및 `디아나 님……!!` 이후 이벤트 진행 정상
- `한겔그`, `김 깅가남`, `해리` 표기와 V2 어설트 버스터 MA 아이콘, `사전` UI 정상

## 배포 파일

- `monoeye_ko_expanded_v1.1.xdelta`
- xdelta SHA-256: `F114AC45D2EFAD024537C452FD5924D972C02AC8F92BB7BE5C06456EB6E6C8C4`
- 크기: `1,602,926` bytes
- VCDIFF header indicator: `0x00`
- xdelta round-trip: PASS

패치는 **합법적으로 소유한 일본판 원본 ROM**에 적용해야 합니다.

지원 원본 SHA-256:

`376E4C6B4B81CC3A7DCEB15DC4B7D0AF04D3E6C8B81E8572569C39D3394870A0`

정상 적용 후 ROM SHA-256:

`F62F14D15F3D76AD2EB33E2B55531AB4781D230CB63BCC2348BBD703D8C39BE3`

## 세이브 호환성

v1.1은 ROM 데이터 수정이며 SaveRAM 형식 변경은 없습니다. 메인TIP 승격 과정에서도 live SaveRAM은 byte-exact로 보존했습니다. 기존 세이브는 그대로 사용할 수 있지만 업데이트 전 원본 ROM과 세이브 파일을 별도로 백업하는 것을 권장합니다.
