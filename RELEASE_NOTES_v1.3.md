# v1.3

`v1.3`은 `v1.2` 이후 실플레이에서 추가로 확인된 기체 스테이터스 그래픽, 전투대사/시나리오 문장, ID 커맨드 그래픽, 도감 색인 및 도감 아이콘 문제를 정리한 후속 안정화 릴리스입니다. 기존 SaveRAM 형식은 변경하지 않았으며, 모든 메인 승격 과정에서 live SaveRAM을 별도로 보존했습니다.

## 주요 변경 사항

### 기체 스테이터스 `移動` 그래픽 한글화

- 기체 스테이터스 화면의 `移動`을 **`이동`**으로 한글화했습니다.
- 실제 화면에서 사용하는 24×24 packed-4bpp 3×3 타일 구조를 기준으로 수정했습니다.
- 사용자 실측 승인 후보의 288바이트 그래픽을 그대로 메인TIP에 반영했습니다.

### 기체명 `ビグ・ザム` 표준화

- 도감/스테이터스에서 `비그・자무`로 표시되던 두 변형을 **`빅 잠`**으로 통일했습니다.
- plain 이름과 status-marker 이름을 모두 수정했으며, 상태 아이콘 raw marker는 보존했습니다.

### 전투대사 후속 교정

- `トビアっ！！`의 기계적 번역 `토비아윽！！`을 **`토비앗！`**으로 교정했습니다.
- 네오 지온 사관 사격 대사에서 `全전 포문 개방！！`처럼 첫 한자 `全`이 중복 노출되던 false-visible-lead 문제를 수정했습니다.
- 동일 구조의 `5D:C23D`, `5E:9885` 두 레코드를 고정폭/terminator 보존 방식으로 정리했습니다.
- 해당 런타임 경로를 dialogue runtime contract의 explicit runtime-proven anchor로 추가했습니다.

### 시나리오 대사 후속 교정

- 20셀 경계에서 잘리던 `제로 무라사메` 대사를 **`제로 무라사메 말인가？`**로 재작성했습니다.
- `그래서、 우리와 와라！ 라일라！！` 계열의 기계번역체와 이름 오표기를 **`그러니、 같이 가자！ 레이라！！`**로 수정했습니다.
- 해리 오드의 외침 `해리 오드오오오！！`을 **`해리 오드으으으！`**로 교정했습니다.
- 기존 시나리오 레코드 구조와 terminator는 그대로 보존하고 private helper/ext3의 실제 표시문구만 교정했습니다.

### ID 커맨드 `선제` 그래픽 계열 수정

- 기존 메인TIP에서 깨져 보이던 `선제` ID 커맨드 그래픽을, 사용자 실측에서 본문이 정상적으로 보이는 테스트 계열로 교체했습니다.
- 실제 런타임은 private 9타일 + shared 3타일의 sparse 12타일 구성임을 확인해 기존 8타일 가정을 폐기했습니다.
- ID 커맨드 문구 `말하자면 소방의 바람、윈드라고`를 **`말하자면 불을 끄는 바람、윈드라고`**로 교정했습니다.

> 알려진 이슈: `선제` 우측 하단에 느낌표 하단으로 보이는 잔여 타일이 일부 런타임에서 남습니다. 본문 `선제` 자체가 깨지던 이전 메인TIP보다 개선된 사용자 승인 계열을 v1.3 기준으로 채택했으며, 잔여 타일의 정확한 업로드 경로는 후속 버전에서 별도 수정할 예정입니다.

### 도감 가나 색인 전체 한글화

- MS/캐릭터 도감의 오십음도 색인을 한글화했습니다.
- `아이우에오`, `카키쿠케코`, `사시스세소`, `타치츠테토`, `나니누네노`, `하히후헤호`, `마미무메모`, `야유요`, `라～로、와` 전체 행의 실화면 한글화를 확인했습니다.
- 과거 raw chart 자체를 E5 18 portal로 치환했을 때 감상모드 BGM 목록이 깨졌던 문제를 피하기 위해 원본 `75:B889-B8BF` chart는 byte-exact로 보존했습니다.
- 실제 표시 시점의 glyph 렌더 경로에서만 한글 glyph를 치환하고, 16 MiB 확장 영역의 별도 lookup data를 사용합니다.
- 감상모드 BGM이 참조하는 원본 인덱스 데이터는 변경하지 않았습니다.

### `테라・스오노` MA 아이콘 복구

- 도감의 `테라・스오노`에서 MA 아이콘 좌측 절반이 2개 보이던 문제를 수정했습니다.
- 원본 icon tail `E6C5 E6C9`(좌+우)가 번역 데이터에서 `E6C5 E6C5`(좌+좌)로 평탄화된 것이 원인이었습니다.
- ext3 문구의 마지막 raw tile code 1바이트만 `C5 → C9`로 복구했습니다.
- 재발 방지를 위해 해당 도감 레코드에 `raw_tail_hex: E6C5E6C9` 계약을 추가했습니다.

## 최종 정적 검증

v1.3 최종 메인TIP에서 다음을 다시 확인했습니다.

- 메인TIP 크기: `16,777,216` bytes
- WonderSwan checksum: `AFFB` · 유효
- dialogue runtime contracts: `24,925`건
- active checked: `7,395`건
- quarantine checked: `17,530`건
- hard failure: `0`
- review item: `0`
- terminology audit: active source / dictionary / five-bank dictionary / rendered record 잔여 `0`
- xdelta 원본 → 메인TIP round-trip: byte-exact PASS
- VCDIFF header indicator: `0x00`
- VCDIFF secondary compression/application header: 배포 호환 모드에서 비활성

## 배포 파일

- `monoeye_ko_expanded_v1.3.xdelta`
- xdelta SHA-256: `0471E51F7D1796D840F82B85278F54DAD4A97D833B260A05960CED39C5C15267`
- 크기: `1,615,611` bytes
- xdelta round-trip: PASS

패치는 **합법적으로 소유한 일본판 원본 ROM**에 적용해야 합니다.

지원 원본 SHA-256:

`376E4C6B4B81CC3A7DCEB15DC4B7D0AF04D3E6C8B81E8572569C39D3394870A0`

정상 적용 후 ROM SHA-256:

`26B780799C3C9CF0A554006C1B778025EC57A6F7C3B8FD7279D2CE654350FBC9`

## 세이브 호환성

v1.3은 ROM 데이터 수정이며 SaveRAM 형식 변경은 없습니다. v1.2에서 사용하던 세이브를 그대로 사용할 수 있습니다. 다만 업데이트 전 원본 ROM과 세이브 파일을 별도로 백업하는 것을 권장합니다.
